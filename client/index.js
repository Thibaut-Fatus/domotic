import React from 'react';
import ReactDOM from 'react-dom';

if (module.hot) {
    module.hot.accept(); // eslint-disable-line global-require
}

ReactDOM.render(
  <h1>Hello, world of React 3!</h1>,
  document.getElementById('root')
);
